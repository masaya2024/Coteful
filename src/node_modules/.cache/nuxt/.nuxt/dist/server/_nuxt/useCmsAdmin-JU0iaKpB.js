import { u as useLocale, h as useCookie, i as createAdminRepository } from "../server.mjs";
function formatDateTime(value) {
  if (!value) {
    return "Not available";
  }
  return new Intl.DateTimeFormat("ja-JP", {
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
    hour: "2-digit",
    minute: "2-digit"
  }).format(new Date(value));
}
function formatBytes(value) {
  if (!value) {
    return "0 B";
  }
  const units = ["B", "KB", "MB", "GB"];
  let next = value;
  let unitIndex = 0;
  while (next >= 1024 && unitIndex < units.length - 1) {
    next /= 1024;
    unitIndex += 1;
  }
  return `${next.toFixed(next >= 10 || unitIndex === 0 ? 0 : 1)} ${units[unitIndex]}`;
}
function toSlug(value) {
  return value.trim().toLowerCase().replace(/[^a-z0-9]+/g, "-").replace(/^-+|-+$/g, "");
}
const contentRepository = {
  listContent(modelApiId, query) {
    return $fetch(`/api/v1/content/${modelApiId}`, {
      query
    });
  },
  getContent(modelApiId, entryId, query) {
    return $fetch(`/api/v1/content/${modelApiId}/${entryId}`, {
      query
    });
  },
  listMedia() {
    return $fetch("/api/v1/media");
  },
  getMedia(id) {
    return $fetch(`/api/v1/media/${id}`);
  },
  listModels() {
    return $fetch("/api/v1/admin/models");
  }
};
let draftCounter = 0;
const fieldTypeOptions = [
  { label: "Text", value: "text" },
  { label: "Textarea", value: "textarea" },
  { label: "Rich Text", value: "richText" },
  { label: "Number", value: "number" },
  { label: "Boolean", value: "boolean" },
  { label: "Date", value: "date" },
  { label: "JSON", value: "json" },
  { label: "Media", value: "media" },
  { label: "Reference", value: "reference" }
];
function createFieldDraft(order) {
  return {
    id: `field-draft-${order}-${draftCounter++}`,
    name: "",
    key: "",
    apiKey: "",
    type: "text",
    required: false,
    unique: false,
    isTitle: order === 1,
    showInList: order <= 2,
    validation: {},
    order,
    referenceModelId: null,
    description: ""
  };
}
function normalizeFieldForForm(field) {
  return {
    ...field,
    apiKey: field.apiKey || field.key,
    validation: field.validation || {},
    description: field.description || "",
    referenceModelId: field.referenceModelId || field.validation.referenceModelId || null
  };
}
function defaultFieldValue(type) {
  switch (type) {
    case "number":
      return 0;
    case "boolean":
      return false;
    case "json":
      return "{}";
    default:
      return "";
  }
}
function buildEntryFields(model, current) {
  const result = {};
  for (const field of model?.fields ?? []) {
    result[field.key] = defaultFieldValue(field.type);
  }
  return result;
}
function createEntryFieldDefaults(model) {
  return buildEntryFields(model);
}
function titleField(model) {
  return model?.fields.find((field) => field.key === model.titleFieldKey) || model?.fields.find((field) => field.isTitle) || model?.fields[0] || null;
}
function deriveEntryTitle(model, entryFields, fallback) {
  const targetField = titleField(model);
  if (!targetField) {
    return fallback;
  }
  const value = entryFields[targetField.key];
  return typeof value === "string" && value.length > 0 ? value : fallback;
}
function syncEntryTitle(model, entryFields) {
  return deriveEntryTitle(model, entryFields, "");
}
function syncEntrySlug(input) {
  return toSlug(input);
}
function mediaLabel(media) {
  return `${media.fileName} · ${media.mimeType}`;
}
function referenceEntriesForField(field, entries) {
  const referenceModelId = field.referenceModelId || field.validation.referenceModelId;
  if (!referenceModelId) {
    return entries;
  }
  return entries.filter((entry) => entry.modelId === referenceModelId);
}
function tagsToString(tags) {
  return tags?.join(", ") || "";
}
function useCmsAdmin() {
  const { t } = useLocale();
  const projectId = useCookie("conteful-project-id", {
    sameSite: "lax"
  });
  const adminRepository = createAdminRepository(() => projectId.value);
  return {
    adminRepository,
    contentRepository,
    fieldTypeOptions: fieldTypeOptions.map((option) => ({
      ...option,
      label: t(`fieldType.${option.value}`)
    })),
    entryStatusOptions: [
      { label: t("status.draft"), value: "draft" },
      { label: t("status.published"), value: "published" }
    ],
    modelStatusOptions: [
      { label: t("status.active"), value: "active" },
      { label: t("status.draft"), value: "draft" }
    ],
    tokenKindOptions: [
      { label: t("token.delivery"), value: "delivery" },
      { label: t("token.management"), value: "management" }
    ]
  };
}
export {
  formatBytes as a,
  tagsToString as b,
  fieldTypeOptions as c,
  createFieldDraft as d,
  createEntryFieldDefaults as e,
  formatDateTime as f,
  syncEntrySlug as g,
  mediaLabel as m,
  normalizeFieldForForm as n,
  referenceEntriesForField as r,
  syncEntryTitle as s,
  toSlug as t,
  useCmsAdmin as u
};
//# sourceMappingURL=useCmsAdmin-JU0iaKpB.js.map
